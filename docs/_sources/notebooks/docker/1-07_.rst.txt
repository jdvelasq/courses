Aplicaciones multi-contenedor (pendiente)
=========================================================================================


docker network create my_network

docker volume create  my_volume




docker run  \
     --network my_network  \
     -v my_volumne:/var/lib/mysql \
     -e MYSQL_ROOT_PASSWORD=secret \
     -e MYSQL_DATABASE=todos \
     -d mysql:5.7